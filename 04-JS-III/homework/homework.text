las arrays (arreglos), estas son como las variables que vimos solo que estas pueden almacenar una serie ordenada de valores de distintos tipos dentro de esta, es ordenada ya que tiene un orden que empieza por el 0 hasta el número que queramos.